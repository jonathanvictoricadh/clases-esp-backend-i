server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jonathanvictoricadh/clases-esp-backend-i
          default-label: clase-10-testing
          force-pull: true
          search-paths: configData
