server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jonathanvictoricadh/clases-esp-backend-i
          default-label: clase-3-FeignLoadBalance
          force-pull: true
          search-paths: configData
